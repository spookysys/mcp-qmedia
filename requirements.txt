# Runtime dependencies for this server's .venv.
#
#   uv pip install -r requirements.txt
#
# Small on purpose: since the captioning service (mcp-caption, :8937) took over
# the models, the API keys and the ffmpeg handling, this process only speaks MCP
# on stdio and HTTP to loopback. It no longer serves a web page, so it no longer
# needs mcp-web-core's browser-facing rules either.
mcp>=1.9,<2
httpx
